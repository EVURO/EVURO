{
  "name": "evuro-admin-panel",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "apps/evuro-admin-panel/src",
  "projectType": "application",
  "targets": {
    "build": {
      "executor": "@nx/vite:build",
      "outputs": ["{options.outputPath}"],
      "defaultConfiguration": "production",
      "options": {
        "outputPath": "dist/apps/evuro-admin-panel"
      },
      "configurations": {
        "development": {
          "mode": "development"
        },
        "production": {
          "mode": "production"
        }
      }
    },
    "serve": {
      "executor": "@nx/vite:dev-server",
      "defaultConfiguration": "development",
      "options": {
        "buildTarget": "evuro-admin-panel:build"
      },
      "configurations": {
        "development": {
          "buildTarget": "evuro-admin-panel:build:development",
          "hmr": true
        },
        "production": {
          "buildTarget": "evuro-admin-panel:build:production",
          "hmr": false
        }
      }
    },
    "preview": {
      "executor": "@nx/vite:preview-server",
      "defaultConfiguration": "development",
      "options": {
        "buildTarget": "evuro-admin-panel:build"
      },
      "configurations": {
        "development": {
          "buildTarget": "evuro-admin-panel:build:development"
        },
        "production": {
          "buildTarget": "evuro-admin-panel:build:production"
        }
      }
    },
    "lint": {
      "executor": "@nx/eslint:lint",
      "outputs": ["{options.outputFile}"],
      "options": {
        "lintFilePatterns": ["apps/evuro-admin-panel/**/*.{ts,tsx,js,jsx}"]
      }
    }
  },
  "tags": []
}
